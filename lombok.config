# Lombok config for Spring Boot
# Why: @RequiredArgsConstructor 등이 생성하는 생성자에 @Qualifier 등의 어노테이션이 복사되도록 강제한다.
# 이를 누락할 경우 @Qualifier가 무시되어 의도치 않은 빈이 주입되는 Silent Bug가 발생할 수 있다.

lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
config.stopBubbling = true
